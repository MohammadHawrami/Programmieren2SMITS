public class Test {
    /*
    -@Test => Kennzeichnung einer Methode als Test
    -@BeforEach => Methode wird vor den Test ausgeführt(Test-Setup)
    -@AfterEach => Methode wird nach dem Test ausgeführt(Test-Teardown)
    -@Disabled => Test wird ignoriet und nicht ausgeführt
    -@DisplayName => Name der Methode in der Anzeige

    Erwartungen vom Test durch ASSERTIONS ausgedrückt:
    -ligen in Klasse gekennzeichnet durch -> org.junit.jupiter oder .api.Assertions

    -assertArrayEquals  -> Element des Arrays sind gleich
    -assertEquals, assertNotEquals  -> Paramter sind gleich / ungleich
    -assertSame, assertNotSame  -> Objekt sind identisch / nicht identisch
    -assertNull, assertNotNull  -> Referenz ist null / nicht null
    -assertTrue, assertFalse  -> Bedingung ist wahr / falsch
    -fail  -> schlägt auf jeden Fall fehl
     */
}
